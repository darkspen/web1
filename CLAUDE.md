# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a bare repository with no application code yet. It contains only a `README.md` with the project name `web1`. No build system, framework, package manager, or source files have been set up.

When code is added, update this file with:
- Build, lint, and test commands
- How to run a single test
- High-level architecture and module relationships
- Key conventions (state management, routing, API patterns)
